require("nvim-tree").setup {
    view = {
        side = "left",
        number = true,
    },
    renderer = {
        add_trailing = true,
        indent_markers = {
            enable = true,
        }
    }
}
